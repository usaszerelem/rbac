{
    "jwtPrivateKey": "AIRFIND_MS_ROLES_JWTPRIVATEKEY",
    "tokenExpirationMs": "AIRFIND_MS_ROLES_TOKEN_EXPIRATION_MS",
    "Environment": "NODE_ENV",
    "db": {
        "host": "MONGO_HOST"
    },

    "cache": {
        "enabled": "REDIS_ENABLED",
        "host": "REDIS_HOST",
        "port": "REDIS_PORT",
        "password": "REDIS_PASSWORD"
    }
}
